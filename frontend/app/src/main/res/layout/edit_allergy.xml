<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="30dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_marginTop="2dp"
    android:layout_marginBottom="2dp"
    android:layout_marginStart="4dp"
    android:layout_marginEnd="4dp">

    <CheckBox
        android:id="@+id/cbox"
        android:layout_width="60dp"
        android:layout_height="30dp"
        tools:layout_editor_absoluteY="0dp" />

    <TextView
        android:id="@+id/medicine_edit"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@+id/cbox"
        tools:layout_editor_absoluteY="5dp"
        tools:text="medicine" />

    <TextView
        android:id="@+id/material_edit"
        android:layout_width="120dp"
        android:layout_height="wrap_content"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@+id/medicine_edit"
        tools:layout_editor_absoluteY="5dp"
        tools:text="material" />

    <TextView
        android:id="@+id/symptom_edit"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:textSize="15sp"
        android:textStyle="bold"
        app:layout_constraintStart_toEndOf="@+id/material_edit"
        tools:layout_editor_absoluteY="5dp"
        tools:text="symptom" />
</androidx.constraintlayout.widget.ConstraintLayout>